<?xml version="1.0" encoding="utf-8"?>
<!-- Config for BlockerAccessibilityService.
     - typeWindowStateChanged: detect a blocked APP coming to the foreground.
     - typeWindowContentChanged + canRetrieveWindowContent + flagReportViewIds: read ONLY the
       browser URL bar (by view id) to detect blocked SITES. Nothing is stored or transmitted. -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged|typeViewTextChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagReportViewIds"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100"
    android:description="@string/accessibility_service_description" />
